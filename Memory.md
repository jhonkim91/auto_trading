# Memory.md

## 현재 프로젝트 상태
- Tkinter 기반 한국투자증권 KIS 자동매매 앱이다.
- 권장 GUI 실행 경로는 `run_app.bat` -> `autotrader.py`이다.
- 대시보드 보유종목/잔액 표시는 국내 원화 잔고만 보던 상태에서 국내 원화 + 미국 달러 잔고를 분리 표시하도록 수정했다.
- 매매 로직의 `safe_balance()`는 국내 매매 판단용으로 유지하고, 화면/리포트 전용 `portfolio_balance()`에서 해외 잔고를 추가 조회한다.
- 설정 탭은 `KIS_PAPER_ACCOUNT`와 `KIS_REAL_ACCOUNT`를 별도 입력/저장하고, `TRADING_MODE`에 따라 선택된 계좌만 KIS API 호출에 적용한다.
- 설정 저장 후 기존 엔진/봇 인스턴스는 정지·초기화되어 이전 모드/계좌가 계속 사용되지 않도록 했다.
- 상단 엔진 버튼 옆 계좌 전환 버튼을 추가했다. 모의 화면은 파란 배너, 실전 화면은 빨간 경고 배너로 완전히 다르게 표시한다.
- 매매일지/일일·주간 리포트/실패기록 정리는 현재 `TRADING_MODE`의 기록만 조회하도록 분리했다.

## 변경 파일
- [x] `autotrader.py`: 국내/미국 표시용 포트폴리오 스냅샷, USD 잔고/평가금액, 통화별 대시보드 렌더링 추가
- [x] `src/kis_api.py`: 모듈 분리형 KIS 해외 잔고 조회에 `cash`, `total_eval`, 통화/시장 메타데이터 추가
- [x] `src/trader.py`: 모듈 분리형 표시용 포트폴리오 스냅샷 및 리포트 반영
- [x] `src/config.py`: `.env` 재로드 및 모의/실전 계좌 원문 보관
- [x] `app.py`: 모듈 분리형 GUI 대시보드에서 `portfolio_balance()` 사용, 모의/실전 계좌 분리 저장
- [x] `autotrader.py`: 상단 계좌 전환 버튼, 모드별 배너, 모드별 매매일지/리포트 필터링 추가
- [x] `README.md`: 대시보드 표시 및 모의/실전 계좌 분리 적용 설명 갱신
- [x] `tests/test_portfolio_snapshot.py`: 네트워크 없이 포트폴리오 병합/표시 포맷, 활성 계좌 키, 매매일지 모드 필터링 검증

## 최신 검증 결과
- [x] `py -m unittest discover -s tests -p "test_*.py"`: 5 tests OK
- [x] `py -m compileall autotrader.py app.py src tests`: OK
- [x] `git diff --check`: 종료 코드 0, CRLF 변환 경고만 표시

## 남은 작업
- [ ] 현재 실행 중인 `autotrader.py` 프로세스는 이전 코드를 사용 중이므로, 변경 반영은 GUI 재시작 후 확인한다.
- [ ] GUI 재시작 후 상단 계좌 전환 버튼과 모드별 배너가 의도대로 보이는지 확인한다.
- [ ] 설정 탭에서 모의/실전 계좌가 분리 입력되는지, 선택 모드의 계좌만 적용되는지 확인한다.
- [ ] 매매일지와 리포트가 현재 모드 기록만 보여주는지 확인한다.
- [ ] 대시보드 새로고침으로 실제 KIS 모의 국내/미국 잔고 표시를 확인한다.

## 주의 사항
- 이번 작업에서 실전 주문, 청산, 계좌 자금 이동, 원격 push는 수행하지 않았다.
- 실제 API 키, 토큰, 계좌번호 등 시크릿 값은 기록하지 않는다.
